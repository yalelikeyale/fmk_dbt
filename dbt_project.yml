
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'fmk_dbt_project'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'fmk_dbt_user'

# These configurations specify where dbt should look for different types of files.
# The `source-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_modules"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

vars:
  'heap:timezone': 'America/Denver'
  'heap:topline_funnel': ['marketing_site_landing_page_viewed_home','user_created','game_events_game_viewed_start','game_events_game_clicked_play','game_events_game_submitted_round','game_completed']
  'heap:influence_events': ['marketing_site_docs_viewed_docs']
  'heap:signup_event': 'user_created'
  'heap:conversion_event': 'game_completed'

# In this example config, we tell dbt to build all models in the example/ directory
# as tables. These settings can be overridden in the individual model files
# using the `{{ config(...) }}` macro.
models:
  fmk_dbt_project:
    # Config indicated by + and applies to all files under models/example/
    sources:
      materialized: ephemeral
    staging:
      heap:
        +materialized: incremental
        heap_events_fct:
          enabled: true
        heap_pageviews_fct:
          enabled: false
        heap_sessions_fct:
          enabled: true
        heap_users_dim:
          enabled: false
    fmk_core:
      enabled: false
    explores:
      enabled: false
